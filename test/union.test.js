// @flow

// const union = require('../union')
// const literal = require('../literal')

// const a = literal('a')
// const b = literal('b')

test('union', async () => {
  // await expect(union(a, b)('a')).resolves.toEqual([ '', 'a' ])
  // await expect(union(a, b)('b')).resolves.toEqual([ '', 'b' ])
  // await expect(union(a, b)('c')).rejects.toThrowError('c')
})
